# Docker

Pasta destinada aos projetos de Docker dos alunos.

Cada aluno deve criar sua própria subpasta com nome e sobrenome, por exemplo:

```
DOCKER/
└── joao-silva/
    └── (arquivos do projeto)
```
