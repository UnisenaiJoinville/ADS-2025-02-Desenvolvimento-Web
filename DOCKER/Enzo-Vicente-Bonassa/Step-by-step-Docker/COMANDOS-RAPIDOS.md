# Comandos rápidos
```bash
docker --version
docker run hello-world
docker ps
docker ps -a
docker images
docker volume ls
docker network ls
docker stats
```

## Compose
```bash
docker compose up
docker compose up -d
docker compose ps
docker compose logs
docker compose down
```

## Limpeza
```bash
docker container prune
docker image prune
```
