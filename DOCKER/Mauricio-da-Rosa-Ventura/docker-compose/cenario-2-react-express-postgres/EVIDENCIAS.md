# Evidência de execução — Cenário 2

> Este arquivo ainda não tem evidência real. Rode `./coletar-evidencias.sh`
> (com o Docker Desktop/Engine aberto e com internet) a partir desta pasta
> para gerar este arquivo automaticamente com a saída real de `docker
> compose up`, o acesso via proxy Nginx, o pgAdmin subido com `--profile
> tools` e os logs da API.
