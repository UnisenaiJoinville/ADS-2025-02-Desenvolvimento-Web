# Evidência de execução — Cenário 3

> Este arquivo ainda não tem evidência real. Rode `./coletar-evidencias.sh`
> (com o Docker Desktop/Engine aberto e com internet) a partir desta pasta
> para gerar este arquivo automaticamente: sobe a pilha, espera todos os
> healthchecks, dispara um evento de ponta a ponta (frontend → api →
> Postgres + RabbitMQ → worker) e grava os logs e o resultado.
