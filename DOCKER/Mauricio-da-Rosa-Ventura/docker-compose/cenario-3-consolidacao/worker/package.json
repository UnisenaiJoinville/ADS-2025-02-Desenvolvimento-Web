{
  "name": "cenario-3-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Worker do Cenario 3 - consome a fila publicada pela API (POST /eventos) e atualiza o registro no Postgres.",
  "scripts": {
    "worker": "node src/worker.js"
  },
  "dependencies": {
    "amqplib": "^0.10.4",
    "pg": "^8.13.0"
  }
}
