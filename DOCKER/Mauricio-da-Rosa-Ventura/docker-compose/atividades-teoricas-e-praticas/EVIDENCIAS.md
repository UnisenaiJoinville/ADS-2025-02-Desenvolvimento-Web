# Evidência — atividades práticas P2 e P4

> Este arquivo ainda não tem evidência real. Rode `./verificar.sh` (com o
> Docker aberto, e com o Cenário 1 já de pé — `docker compose up -d` na
> pasta `../cenario-1-vue-node-mysql`) para gerar este arquivo
> automaticamente.
