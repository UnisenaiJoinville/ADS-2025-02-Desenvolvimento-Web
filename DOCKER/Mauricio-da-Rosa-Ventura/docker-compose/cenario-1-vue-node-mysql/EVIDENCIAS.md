# Evidência de execução — Cenário 1

> Este arquivo ainda não tem evidência real. Rode `./coletar-evidencias.sh`
> (com o Docker Desktop/Engine aberto e com internet, para baixar as imagens
> oficiais na primeira vez) a partir desta pasta para gerar este arquivo
> automaticamente com a saída real de `docker compose up`, `docker compose
> ps`, os logs da api/rabbitmq/worker e o teste de persistência do MySQL.
