version: '3'
services:
  meu-servico:
    image: nginx
    container_name: meu-servico

  proxy:
    image: nginx
    container_name: proxy
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - meu-servico
