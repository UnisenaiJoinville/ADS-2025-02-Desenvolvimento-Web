server {
    listen 80;

    # "app" e o nome do servico no compose; o DNS do Docker resolve o IP.
    location / {
        proxy_pass http://app:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
