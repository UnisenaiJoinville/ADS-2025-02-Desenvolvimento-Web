const name = "Consulta";
const price = 150;
const durationMinutes = 45;

const message = `Serviço: ${name} | Preço: R$ ${price} | Duração: ${durationMinutes} minutos`;

console.log(message);

/*Template literals permitem inserir variáveis diretamente dentro de uma string usando ${}, 
deixando o código mais legível e evitando várias concatenações.*/ 